services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      --.:/app
      - /app/node_modules
    environment:
    - CHOKIDAR_USEPOLLING=true #GARANTE O HOT RELOAD EM ALGUNS SISTEMAS
    stdin_open: true
    tty: true
    
